

//assign variables for the 19-120 number (targetNum), the number (1-12) for each crystal, wins, losses, and total score

var targetNum = 0;
var crystalNum = 0;
var wins = 0;
var losses = 0;
var numSoFar = 0;

//use JQ to code outputs for each

$("#number-to-guess").text(targetNum);

//onClick, start game

//generate random targetNum

for (var i = 0; i >18 && i < 121; i++) {
    var random = Math.floor(Math.random()) + 1;);
}

//generate random crystalNum x4 (one for each crystal) -- use JQ .each function?
// //$( document.body ).click(function() {
//     $( "div" ).each(function( i )

//document.log click value into numSoFar

//repeat until numSoFar = targetNum then Wins +1

//OR numSoFar > targetNum then Losses +1










